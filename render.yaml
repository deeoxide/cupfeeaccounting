services:
  - type: web
    name: sme-cloud-accounting
    runtime: node
    buildCommand: npm run install:all && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 24.16.0
      - key: PORT
        value: 3000
